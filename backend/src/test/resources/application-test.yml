spring:
  datasource:
    url: jdbc:h2:mem:ecommerce_agent_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
  sql:
    init:
      mode: never

app:
  jwt:
    secret: test-secret-that-is-at-least-32-characters-long
    expiration-ms: 3600000
